/** @type {import('next').NextConfig} */
const nextConfig = {
  outputFileTracingIncludes: {
    "/prototype/bluefairy-world": ["./prototype/bluefairy-world.html"],
  },
};

export default nextConfig;
