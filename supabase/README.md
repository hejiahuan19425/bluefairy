# Supabase

Project URL: `https://ddhwonsotxgifeejblum.supabase.co`

Run `schema.sql` in the Supabase SQL Editor before enabling live M2 routes.

Only `SUPABASE_SERVICE_ROLE_KEY` may access these tables for now. Keep it in `.env.local` / Vercel environment variables only.
